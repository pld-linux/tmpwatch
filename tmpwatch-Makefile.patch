diff -ru tmpwatch-1.7.old/Makefile tmpwatch-1.7/Makefile
--- tmpwatch-1.7.old/Makefile	Thu May 20 00:44:17 1999
+++ tmpwatch-1.7/Makefile	Thu May 20 00:59:07 1999
@@ -3,13 +3,19 @@
 
 CFLAGS=$(RPM_OPT_FLAGS) -Wall -DVERSION=\"$(VERSION)\"
 
+prefix=/usr
+sbindir=$(prefix)/sbin
+bindir=$(prefix)/bin
+mandir=$(prefix)/man
+DESTDIR=
+
 all: tmpwatch
 
 install:
-	[ -d $(PREFIX)/usr/sbin ] || mkdir -p $(PREFIX)/usr/sbin
-	[ -d $(PREFIX)/usr/man/man8 ] || mkdir -p $(PREFIX)/usr/man/man8
-	install -s -m 755 tmpwatch $(PREFIX)/usr/sbin/tmpwatch
-	install -s -m 644 tmpwatch.8 $(PREFIX)/usr/man/man8/tmpwatch.8
+	[ -d $(DESTDIR)$(sbindir) ] || mkdir -p $(DESTDIR)$(sbindir)
+	[ -d $(DESTDIR)$(mandir)/man8 ] || mkdir -p $(DESTDIR)$(mandir)/man8
+	install -s -m 755 tmpwatch $(DESTDIR)$(sbindir)/tmpwatch
+	install -m 644 tmpwatch.8 $(DESTDIR)$(mandir)/man8/tmpwatch.8
 
 clean:
 	rm -f tmpwatch
